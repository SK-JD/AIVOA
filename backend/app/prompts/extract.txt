Extract structured HCP-interaction details from the rep's description below and return a SINGLE
JSON object. Only include information actually present or clearly implied — leave anything unknown
as an empty string or empty list. Do NOT invent names, products, or dates.

Today's date is {{ today }} (format YYYY-MM-DD). Resolve relative dates ("today", "tomorrow",
"in two weeks") against it where they refer to when the interaction happened.

Return JSON with exactly these keys:
{
  "hcp_name": "the HCP's name, e.g. 'Dr. Smith' (string)",
  "interaction_type": "one of: In-person Meeting, Call, Email, Virtual, Conference. Infer from the verb: 'met'/'visited' -> In-person Meeting, 'called'/'phoned' -> Call, 'emailed' -> Email, 'video'/'zoom' -> Virtual. Default 'In-person Meeting'",
  "date": "YYYY-MM-DD if a meeting date is given, else ''",
  "time": "HH:MM (or e.g. '10:30 AM') if a time is given, else ''",
  "duration": "meeting length if mentioned, e.g. '25 minutes', else ''",
  "attendees": [{"name": "person present besides the HCP", "role": "their role e.g. Product Specialist, Regional Manager, Nurse"}],
  "topics": "concise comma-separated discussion points (e.g. 'Product X efficacy, dosage, side effects')",
  "materials_shared": [{"type": "one of: Brochure, Product Leaflet, Clinical Study, Research Paper, Product Catalogue, Presentation Deck, Safety Information, Other Marketing Material", "name": "short label e.g. 'Product X Brochure'"}],
  "samples_distributed": [{"name": "product sample name e.g. 'Product X Sample'", "quantity": 1}],
  "sentiment": "Positive | Neutral | Negative — the HCP's observed/inferred reaction",
  "outcomes": "WHAT HAPPENED as a result of the meeting (e.g. 'Showed interest and accepted brochures', 'Requested more clinical data'). This is the RESULT, not the next step.",
  "followup_actions": "WHAT SHOULD HAPPEN NEXT — concrete next steps the rep will take (e.g. 'Send Phase III PDF; schedule follow-up in 2 weeks'). This is the NEXT ACTION, not what already happened."
}

Materials vs samples: brochures/leaflets/studies/papers/decks are `materials_shared`; physical drug
samples handed over are `samples_distributed`. Keep them separate.
Outcomes vs follow-up: outcomes = the result that already occurred; follow_up = future action.

Description:
{{ description }}
